import sys

L = int(sys.stdin.readline())
PW = []
for _ in range(L):
    PW.append(list(input()))

for i in range(L):
    for j in range(len(PW[i])):
        print(PW[i][j])


def password():
    stack = []
    cnt = 0
    while(cnt <=len(비밀번호 입력 리스트))
    if 들어오는 문자가 '<'인데 :
        if stack 비어있으면:
            cnt += 1
            continue
        else:
            stack에 있는거 한개 pop진행
            cnt +=1
    elif 들어오는 문자가 '>'인데:
        if stack 비어있으면:
            cnt +=1
            continue
        elif
    
    
